package com.myday.expense;

/** Fixed set of expense categories (kept in sync with the frontend dropdown). */
public enum ExpenseCategory {
    FOOD,
    TRAVEL,
    BILLS,
    SHOPPING,
    HEALTH,
    OTHER
}
