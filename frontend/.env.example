# Base URL of the backend API (no trailing slash, no /api suffix).
# Local dev: http://localhost:8080
# Production: your deployed backend URL, e.g. https://myday-api.onrender.com
VITE_API_URL=http://localhost:8080
